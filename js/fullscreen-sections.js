$(document).ready(function(){
	console.log("fullscreen js is loading up");	
	 vpw = $(window).width();
	 vph = $(window).height();
    $('.full-page').height(vph);
    $('.full-page').width(vpw);
})


